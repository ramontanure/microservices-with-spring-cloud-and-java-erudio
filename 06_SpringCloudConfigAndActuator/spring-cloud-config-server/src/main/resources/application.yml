server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ramontanure/ramontanure-config-server
          search-paths: 'greeting-service'
#  profiles:
#    active:
#      - native